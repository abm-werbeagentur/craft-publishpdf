{% extends "_layouts/cp" %}
{% set selectedSubnavItem = 'imhomedia-publishpdf-issuu' %}

{% set title = 'Issuu' %}

{% block content %}
{{ dump(documents) }}
    {# {% if documents is not null %}
        {% if documents.total > 0 %}
            <h2>{{ documents.total}} Documents</h2>
            <table class="data fullwidth"> 
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Title</th>
                        <th>Image</th>
                        <th>Link</th>
                        <th>Pages</th>
                    </tr>
                </thead>

                <tbody>
                    {% for document in documents.documents %}
                        <tr>
                            <td class="thin">{{ document.id }}</td>
                            <td>{{ document.title }}</td>
                            <td class="thin"><img src="{{ document.image.small }}" alt="Image: {{ document.title }}" title="Image: {{ document.title }}" /></td>
                            <td class="thin"><a href="{{ document.url }}" target="_blank" rel="noopener noreferrer">{{ document.short_url }}</a></td>
                            <td class="thin">{{ document.pages }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        {% else %}
            <h2>No documents uploaded to Yumpu</h2>
        {% endif %}
    {% else %}
        <h2>No documents found on Yumpu</h2>
    {% endif %} #}
{% endblock %}